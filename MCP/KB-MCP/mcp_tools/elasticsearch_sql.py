def elasticsearch_sql(query: str) -> str:
    """Execute SQL against Elasticsearch (skeleton).

    Real implementation will live here. Keep any network operations inside
    this function to avoid import-time side effects.
    """
    raise NotImplementedError("elasticsearch_sql is not yet implemented in the split package")
